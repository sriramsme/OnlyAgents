# skills/websearch.yaml
name: websearch
type: native
enabled: true
access_level: read
description: Search the web, gather information, fetch URLs, and summarize findings
version: 1.0.0
capabilities:
  - web_search
  - information_gathering
  - url_fetching
connector:
  required: true
  default: duckduckgo
  supported:
    - duckduckgo
    - brave
