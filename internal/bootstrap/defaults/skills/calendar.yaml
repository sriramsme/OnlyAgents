# skills/calendar.yaml
name: calendar
type: native
enabled: true
access_level: write
description: Create, view, and manage calendar events
version: 1.0.0
capabilities:
  - scheduling
  - calendar_management
  - time_management
connector:
  required: false  # has a local connector enabled by default. uses local db.
  default: local_calendar
  supported:
    - local_calendar
    # - google_calendar  (future)
    # - outlook          (future)
