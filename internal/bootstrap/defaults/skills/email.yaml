# skills/email.yaml
name: email
type: native
enabled: true
access_level: write
description: Read, compose, and send emails
version: 1.0.0
capabilities:
  - email_management
  - communication
  - inbox_management
connector:
  required: true
  default: gmail
  supported:
    - gmail
    # - outlook  (future)
    # - imap     (future)
