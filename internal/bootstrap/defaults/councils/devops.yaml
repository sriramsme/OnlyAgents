name: devops
description: Infrastructure and operations team for managing deployments, servers, containers, and pipelines.
enabled: false

agents:
  - devops # TODO: new agent — deployments, infra, monitoring
  - general

skills:
  - docker # TODO: CLI skill
  - kubectl # TODO: CLI skill
  - git # TODO: CLI skill
  - github # CLI — ships today
  - make # TODO: CLI skill
  - restic # TODO: CLI skill — backups
  - ssh # TODO: CLI skill — remote server access
  - web_search

connectors:
  - duckduckgo
  - brave
