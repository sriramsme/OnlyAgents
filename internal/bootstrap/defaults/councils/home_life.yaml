name: home_life
description: Life management assistant for the non-work side of things. Handles household tasks, travel planning, shopping, and home automation.
enabled: false

agents:
  - productivity # ships by default — handles calendar, reminders, tasks
  - general

skills:
  - calendar
  - reminders
  - tasks
  - notes
  - web_search
  - weather # CLI — ships today
  - email # TODO: native skill
  - home_assistant # TODO: CLI skill — home automation via HA REST API

connectors:
  - calendar
  - reminders
  - tasks
  - notes
  - duckduckgo
  - gmail # TODO: native connector
