name: OnlyAgents
bus_buffer_size: 256

security:
  execution_mode: restricted # restricted | native
  working_dir: "" # ~/.onlyagents/ by default
  allow_network: true # cli tools can make network calls
  allow_system_bins: true # can use /usr/bin etc


# memory
memory:
  llm:  # llm used for memory summarization
    provider: openai
    model: gpt-5-mini
    api_key_path: openai/api_key

# skill marketplaces
marketplaces:
  - name: clawhub
    enabled: true
    url: https://clawhub.ai
    vault_paths:
      api_key:
        path: clawhub/api_key
        prompt: ClawHub API key
